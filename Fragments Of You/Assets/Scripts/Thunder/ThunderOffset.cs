using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThunderOffset : MonoBehaviour
{
    public float Offset;
    void Awake()
    {
        //this.gameObject.transform.localPosition = new Vector2(this.transform.localPosition.x + Random.Range(-Offset, Offset), 0);
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
